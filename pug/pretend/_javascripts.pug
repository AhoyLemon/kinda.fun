.javascripts
  if testing
    script(src="js/libraries/vue.full.js")
    script.
      const testing = true;
      const canCheat = true;
  else
    script(src="js/libraries/vue.min.js")
    script.
      const testing = false;
      const canCheat = false;

  //- Socket.io
  script(src="/socket.io/socket.io.js")
  script.
    var socket = io.connect();

  script(src="js/min/pretend.min.js")