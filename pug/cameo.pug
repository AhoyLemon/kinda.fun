- var pageTitle = "";
include cameo/_variables
html
  head
    - var testing = true 
    title kinda.fun CAMEO
    meta(name="viewport" content="width=device-width, initial-scale=1")
    
    link(rel="preconnect" href="https://fonts.gstatic.com")
    link(href="https://fonts.googleapis.com/css2?family=Big+Shoulders+Display:wght@400;700&family=Noto+Serif+JP:wght@400;700&display=swap" rel="stylesheet")

    link(href="css/cameo.css" type="text/css" rel="stylesheet")
body
  #app(v-cloak)


    include cameo/_title-screen

    include cameo/_sort-three

    include cameo/_final-round

    include cameo/_email

    include cameo/_hud

    include cameo/_game-over

		
  if testing
    script(src="js/libraries/vue.full.js")
    script.
      const testing = true;
  else
    script(src="js/libraries/vue.min.js")
    script.
      const testing = false;

  script(src="js/libraries/jquery.min.js")

  script(src="//cdn.jsdelivr.net/npm/sortablejs@1.8.4/Sortable.min.js")
  script(src="//cdnjs.cloudflare.com/ajax/libs/Vue.Draggable/2.20.0/vuedraggable.umd.min.js")

  script(src="js/libraries/vue-toast-notification.min.js")
  link(href="css/libraries/vue-toast-notification.css" type="text/css" rel="stylesheet")

  script(src="js/min/cameo.min.js?updated"+lastUpdated)