section.sermon(v-if="ui.view === 'sermon'")

  .inner
  h2 What will you preach today?
  br
  form(@submit.prevent="defineSermon()")

    fieldset
      label.required First Topic
      Multiselect(
        v-model="my.selectedTopics[0]"
        :options="provideTopicOptions(0)"
        :searchable="true"
        :required="true"
        value-prop="id"
        track-by="title"
        label="title"
        mode="single"
      )
      //.explainer(v-if="my.selectedTopics[0]")
        p {{showThemeDescription(my.selectedTopics[0])}}

    fieldset
      label.required Second Topic
      Multiselect(
        v-model="my.selectedTopics[1]"
        :options="provideTopicOptions(1)"
        :searchable="true"
        :required="true"
        value-prop="id"
        track-by="title"
        label="title"
        mode="single"
      )
      //.explainer(v-if="my.selectedTopics[1]")
        p {{showThemeDescription(my.selectedTopics[1])}}
    fieldset
      label.required Third Topic
      Multiselect(
        v-model="my.selectedTopics[2]"
        :options="provideTopicOptions(2)"
        :searchable="true"
        :required="true"
        value-prop="id"
        track-by="title"
        label="title"
        mode="single"
      )
      //.explainer(v-if="my.selectedTopics[2]")
        p {{showThemeDescription(my.selectedTopics[2])}}

    .button-holder
      button(type="submit" :disabled="!my.selectedTopics[0] || !my.selectedTopics[1] || !my.selectedTopics[2]") Looks Good